services:
  - type: web
    name: echo-voice-assistant-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.run:app
    envVars:
      - key: PORT
        value: 10000
